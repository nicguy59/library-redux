    <div class="row">
    <div id="leftCol" class="col-xs-9">
      <h3><%= item.item_type_name %>: <%= item.title %></h3>
      <% if item.completed? %>
        <p>This item has been completed.</p>
      <% else %>
       <p>Still need to read/watch this.</p>
      <% end %>
    </div>

    <div id="rightcol" class="col-xs-3">
      <p><%= image_tag(item.image_url, alt: "", class: 'thumb')  %></p> 
    </div>
  </div>  

  <div class="row">
   <div class="col-xs-12">
  
    <p>
    <%= link_to 'Edit', edit_item_type_item_path(item.item_type, item) %>
    *
    <%= link_to 'Delete', item_type_item_path(item.item_type, item), method: :delete %>
    </p>

    <p>Click here for more about: <%= link_to(item.title, item_type_item_path(item.item_type, item)) %>
    </p>
    <hr />
   </div> 
  </div>
 